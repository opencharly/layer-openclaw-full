# 2026.246.0200 — chore: advance the layer-gogcli pin to the module-path fix

## Summary

Advance the composed `layer-gogcli` pin from `v2026.243.0409` to
`v2026.246.1816` — the tag carrying the module-path fix
(opencharly/layer-gogcli#3: `github.com/steipete/gogcli` → `github.com/openclaw/gogcli`).
The metalayer's own content is otherwise unchanged.

The candy's own CalVer moves because changed content must ship under a new version.

## How tested

```
$ /tmp/charly-ci/bin/charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds
(v2026.238.1242). The diff is confined to `charly.yml`: the layer-gogcli pin plus
the version line. No packages, services, plan steps or scripts change, so there is
no runtime path to exercise in this repo; the composed gogcli build's runtime path
is proven by the consuming `check-openclaw-full-pod` bed in the distro-cachyos
repo.

## Rulebook compliance

- **R0 (skills)** - `/charly-image:layer` for the candy/ref model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the pin advance is the source-level fix: the metalayer must compose
  the fixed gogcli tag for the openclaw-full image to build.
- **R2 (no deferral)** - the pin ships now, in the same batch as the gogcli fix.
- **R3 (no duplication)** - pins advanced in place; nothing restated locally.
- **R4 (no workarounds)** - the pin is advanced, not the warning allowlisted.
- **R5 (hard cutover)** - the old pin is gone; no old path remains.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` on the committed tree with
  the repo's own pinned charly.
- **R8 (generation)** - N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - N/A: `description:` and `plan:` untouched.
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy ref pin bump + the candy's own CalVer bump. Config only.
- **Verification gate:** `charly box validate` under the repo's pinned charly, exit 0
- **Attribution tier:** `analysed on a live system` - the gate was executed on a
  real checkout with the exit code recorded; no image is built in this repo

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
