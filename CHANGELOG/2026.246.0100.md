# 2026.246.0100 — chore: advance pod-openclaw pin to the OpenClaw 2.0 tag

## Summary

Advance the composed `pod-openclaw` pin from `v2026.243.0411` to
`v2026.246.1617` — the tag that carries the **OpenClaw 2.0** bump (npm
`openclaw@2026.8.2`, opencharly/pod-openclaw#3). The metalayer's own content is
unchanged: it still composes the same 27 candies, and the gateway's port
reachability check rides along from the composed openclaw candy (which now also
asserts the `/healthz` liveness endpoint).

The candy's own CalVer moves because changed content must ship under a new version
rather than reusing one already released.

## How tested

```
$ charly box validate
exit 0
```

Run with the charly version this repo's own `deploy.yml` clones and builds. The diff
is confined to `charly.yml`: the pod-openclaw pin plus the version line and a
description touch. No packages, services, plan steps or scripts change, so there is no
runtime path to exercise in this repo; the composed gateway's runtime path is proven by
the consuming `check-openclaw-full-pod` bed in the charly repo.

## Rulebook compliance

- **R0 (skills)** - `/charly-image:layer` for the candy/ref model;
  `/charly-internals:git-workflow` for PR-only landing and tag-on-merge.
- **R1 (RCA)** - the pin advance is the source-level fix for the 2.0 upgrade: the
  metalayer must compose the new gateway tag for the boxes to carry 2.0.
- **R2 (no deferral)** - the pin ships now, in the same batch as the pod-openclaw bump.
- **R3 (no duplication)** - pins advanced in place; nothing restated locally.
- **R4 (no workarounds)** - the pin is advanced rather than the warning allowlisted.
- **R5 (hard cutover)** - the old pin is gone; no old path remains.
- **R6 (git safety)** - feature branch, no force-push, no hook bypass.
- **R7 / R10 (prove the gate)** - `charly box validate` on the committed tree with the
  repo's own pinned charly.
- **R8 (generation)** - N/A: no generated artifact, no CUE input in this repo.
- **R9 (artifact integrity)** - N/A: no image is built here.
- **ADE (candy description/plan)** - `description:` updated in lockstep with the change
  it describes; `plan:` untouched.
- **SDD (CUE source)** - N/A: no schema authored or generated here.

## Change classification

- **Change class:** candy ref pin bump + the candy's own CalVer bump. Config only.
- **Verification gate:** `charly box validate` under the repo's pinned charly, exit 0
- **Attribution tier:** `analysed on a live system` - the gate was executed on a real
  checkout with the exit code recorded; no image is built in this repo

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*
